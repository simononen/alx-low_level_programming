#include "main.h"
/**
 * print_line - prints line with n characters
 *
 * @n: int parameter for number of characters
 *
 * Return: Nothing (void)
 */

void print_line(int n)
{
	int i;
	int num = n

	if (num <= 0)
	{
		_putchar('\n');
	}
	else
	{
		i = 1;
		while (i <= num)
		{
			_putchar('_';)
			i++;
		}
		_putchar('\n');
	}
}
