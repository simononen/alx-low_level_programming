#include "main.h"
#include <stdlib.h>
/**
 * _abs - returns the absolute value of an integer parameter
 *
 * @n: parameter of type int
 *
 * Return: Returns abs(n) the absolute value
 */
int _abs(int n)
{
	return (abs(n));
}
